
# Plan - Ivoire Hub

## Phases completees

### Phase 3 -- Decoupe des fichiers geants ✅
- `SuiviCandidature.tsx` decoupe (hook + 2 composants)
- `startup/Profile.tsx` decoupe (hook + 3 onglets)
- `Postuler.tsx` decoupe (schema + hook + 4 steps + dialog)
- `admin/Dashboard.tsx` decoupe (hook + tabs + dialogs)
- Constantes centralisees dans `src/lib/constants/startup.ts`

### Phase 4 -- Unification des Layouts et Headers ✅
- `DashboardLayout` generique cree, 3 layouts simplifies en wrappers
- `DashboardHeader` generique cree, 3 headers simplifies en config wrappers
- `DashboardSidebar` generique cree, 3 sidebars simplifies en config wrappers
- `investors/` fusionne dans `investor/`, imports mis a jour

## Prochaines phases

A definir selon les priorites du projet.
